<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidmidterm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PlayBoard.BlueBoard"/>
        <activity android:name=".PlayBoard.VmsBoard"/>
        <activity android:name=".Menu.Waiting"/>
        <activity android:name=".Menu.JoinRoomActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Menu.MenuActivity" />
        <activity android:name=".Login.LoginActivity" />
        <activity android:name=".Menu.Ranking.RankingActivity" />
        <activity android:name=".Menu.CreateRoomActivity" />
        <activity android:name=".Register.RegisterActivity" />
        <activity android:name=".PlayBoard.ClassicBoard" />
        <activity android:name=".SplashScreen.SplashScreenActivity" >


        <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>

</manifest>